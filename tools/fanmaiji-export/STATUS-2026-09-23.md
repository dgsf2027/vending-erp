# A 机每日销售导出准备记录

核验时间：2026-09-23 11:30，Asia/Shanghai。

**状态：环境及代码已暂存；真实导出未验证，07:30 定时任务未启用。**

## 已落实

- A 机 SSH 入口 `project-mac`，主机 `Mac`。
- 独立运行根目录：`/Users/yh-1/fanmaiji-export-run`；主要运行目录权限为 700。
- 代码：`code/tools/fanmaiji-export`；与售卖机生产主仓分开。
- 独立 Python 3.9.6 环境及固定依赖已安装，`pip check` 通过；Chromium 离线启动通过，测试过程中阻断网络。
- 默认范围是上海时区昨日 00:00:00–23:59:59，仅销售明细。9 月 23 日的离线计划正确给出 9 月 22 日全天。
- 两层互斥锁、15 分钟任务超时、逐次日志和 `run/last-status.json` 已实现。下载拒绝空文件、HTML 和无效 XLSX，并保留重复下载的原件。
- 本机和 A 机各自通过 18 项离线检查，覆盖日期边界、未验证配置拦截、查询后日期被重置、文件有效性、错误信息遮蔽、并发与超时。
- 07:30 LaunchAgent 配置暂存于 `launchd/com.aole.fanmaiji-export.plist`，语法检查通过；没有复制到 `~/Library/LaunchAgents`，没有加载。
- A 机原售卖机主仓保持 `main@72570d8` 且工作树干净；现有财务抓取和自动部署未修改。

## 尚未完成及原因

浏览器工具因当前网址的访问策略终止页面操作，本轮未能进行真实登录、日期查询或下载核验。未尝试通过其他网络通道绕过该限制。

登录成功标记、销售页面的日期控件及导出行为仍待核实。`config.yaml` 中相关 `verified` 均为 `false`，脚本会在读取凭据或启动浏览器前停止，防止将后台默认日期报表误当昨日销售。

运行目录尚无真实 `.env`、销售下载文件或成功抓取记录；离线 `--plan` 不会产生成功抓取状态。ERP 自动上传和入账未实现、未执行。本地分支为 `codex/fanmaiji-daily-export`，未推送远程或合入生产主分支。

## 启用前剩余验收

1. 在允许访问的操作环境中验证正常登录流程；若出现验证码或协议确认，由人工处理。
2. 核实销售页面及唯一日期控件，实际查询昨日完整时间范围；只有验证通过的配置才标记为已核实。
3. 下载真实原始文件，核对表头、记录日期、数量和金额与后台同范围结果一致。无销量日也须通过有效表头和后台查询结果确认。
4. 在 A 机独立环境验证一次完整任务，确认成功状态、文件归档及失败处理，再启用暂存的 07:30 LaunchAgent。
5. 文件链路稳定后，另接 ERP 上传/预览及必要的认证；在完成数据和幂等核验前保持人工确认入账。

环境证据位于 A 机 `logs/environment-verification-20260923.json`；公开依赖安装记录位于 `logs/dependency-install-20260923.log`。
