# A 机每日抓取与 vend 导入验收

最新核验：2026-09-23 17:44，Asia/Shanghai。用户已明确要求每天 **07:00** 抓取昨天销售并放入 vend，现已接通完整流程。

## 目前生效的配置

- 复用 A 机 Chrome，07:00 的 LaunchAgent 已加载；原 07:30 已改为 07:00。
- `run_daily.py → sync_daily.py` 串联下载、原始 XLS 校验和 vend 正规 API 导入。
- vend 使用正规注册的 `fanmaiji_daily_sync` 店员账号，每次正常登录；凭据仅存 A 机私有文件。
- 源站正常登录续期已验证：仅确认同源登录页与完整表单后，用用户已提供的凭据正常登录一次，再重查数据。普通网络失败不触发重复登录，验证码或登录错误会停止。
- 用户提供的 `/purchase` 是采购页；本次销售批次在同项目 [导入中心](https://vend.vvaix.com/import) 查看，不创建采购单或资金结算。

## 真实验收

| 环节 | 结果 |
|---|---|
| 应用发布 | PR #7 自动部署主提交 `83db37510da9cf0f8656666ed60eb3b54d7897d2`，17:32:51 完成，3/3 容器运行、后端健康 code 200 |
| 首轮新任务 | 17:33:58–17:34:31 因源站会话过期而停止，未导入；已确认跳转 `/login`，不是 vend 导入错误 |
| 首次完整导入 | 17:38:26.890783–17:38:30.610149，launchd 触发，退出码 0 |
| 销售批次 | ID `6`，`IMP-20260923173830-C8C4` |
| 结果 | 338 总行 / 338 成功 / 0 失败 / 0 重复，待绑定 0、待改价 0 |
| 重复执行 | 17:39:50.850591–17:39:53.968000，退出码 0，复用同一批次 6，没有二次确认或新增批次 |
| 浏览器界面 | A Chrome 查看同一生产应用 `/import`，批次、日期、338/338/0/0 与“已导入”可见 |
| 源站登录续期 | 17:44:06–17:44:07 在专用页执行正常登录成功，随后初始销售查询成功；保留用户原页面 |
| 测试 | 本地及 A 机 Python 101 项通过；独立 MySQL 后端 40 项通过，测试容器已清理 |

数据库独立只读核验：9 月 22 日及批次 6 均 **338 行、240 原订单、362 件、1,401.40 元**。原 XLS 与数据库按订单后缀、设备、名称、条码、数量、金额、时间、货道、支付方式及订单类型共 10 字段逐行比较，338/338 完全一致。

ERP 原始归档为 91,136 字节，SHA-256 为 `cfb597415f2c2ef23c2a00c58d2be0aa47f99ca42bd99588780f30d61ae6c28e`，与源 XLS 完全一致。现有 ERP 归档路径虽使用 `.xlsx` 后缀，内容仍为原始 OLE XLS；解析器现按签名识别，没有转码或修改原文件。

证据：

- A 机日志：`/Users/yh-1/fanmaiji-export-run/logs/2026-09-23T173826.890783.log`、`2026-09-23T173950.850591.log`。
- 同步状态：`run/last-sync.json`；逐日回执：`run/imports/vend-sales-2026-09-22.json`。
- 本地界面截图：`outputs/fanmaiji/vend-import-2026-09-22.png`；执行回执：`outputs/fanmaiji/daily-sync-2026-09-23.json`（均不入 Git）。

原始明细与源站首页的 **12.90 元 / 1 单** 差异仍保留，未自行删单、改金额或作资金结算。9 月 24 日 07:00 自然定时尚未发生，以上证明的是实际 launchd 触发链路及重复运行，不表示已长期稳定运行。

---

## 历史：16:32 只导出阶段

下面的 07:30 和“未导入 ERP”仅描述当时状态，已由上述用户后续要求和真实导入结果更新。

记录更新时点：**2026-09-23 16:32，Asia/Shanghai**。

**当前状态：A 机完整 daily 入口已实际下载并验证昨日销售原件；每日 07:30 LaunchAgent 已安装并加载，经 launchd 手动触发验收成功。9 月 24 日首次 07:30 自然触发尚未验证，不能称长期稳定运行。ERP 未导入。**

## 执行环境更正与会话保留

用户一直要求爬虫使用 A 机 Chrome。此前选用本机 Firefox 是执行环境选择错误；那次 Computer Use 拒绝不能推断 A 机爬虫或售货机网站受阻。

A 机现有 Chrome 通过 `127.0.0.1:9222` 提供 CDP，用户目录为 `/tmp/yz-gui-debug`。复用其现有 context，新建任务专用页执行；用户原登录页（标签 ID 前缀 `5814F…`，URL `/index?isFrom=login`）保留。仅清理本次专用页及其 popup 树，不关闭用户其他页面或 Chrome，也不另开空白浏览器。CDP 路径无需 `.env` 或密码。16:31 仅关闭本次人工检查的 `exporterUserTask` 页后，Chrome 恢复 7 个页面，原登录页仍在。

现有财务爬虫今早 07:00:05–07:02:33 四来源成功；售卖机验收证据独立记录如下。更多历史背景见 [浏览器诊断记录](BROWSER-DIAGNOSIS-2026-09-23.md)。

## 实际运行证据

以下时间均为 2026-09-23、Asia/Shanghai；日志路径均位于 A 机。

| 执行 | 时间 | 结果及含义 |
|---|---|---|
| 完整 daily 入口 | 16:19:40.912965–16:19:43.985018 | `success`，退出码 0；实际下载并验证正式归档 XLS |
| 安装后重复下载测试 | 16:20:42 开始 | `failed`；厂家明确返回重复下载限频，未当作成功 |
| launchd 手动触发验收 | 16:29:41.841656–16:29:44.863307 | `success`，退出码 0；重新查询 338 行，校验并复用正式原件，没有重新下载 |

成功日志：

- 实际下载：`/Users/yh-1/fanmaiji-export-run/logs/2026-09-23T161940.912965.log`。
- launchd 触发：`/Users/yh-1/fanmaiji-export-run/logs/2026-09-23T162941.841656.log`。
- 最新任务状态：`/Users/yh-1/fanmaiji-export-run/run/last-status.json`。

16:20:42 的失败响应为 `code=999999`、`status=false`、`message=请勿短时间内重复下载，请稍后再试`。随后增加同日期范围已有原件重新校验后复用，尊重厂家限频，不自动重试下载。这次失败保留在验收记录中；16:29 的成功是修正后的独立执行。

## 已下载及校验的文件

查询范围为 **2026-09-22 00:00:00–23:59:59**。

| 项目 | 结果 |
|---|---|
| A 机正式归档 | `/Users/yh-1/fanmaiji-export-run/downloads/2026-09-23/sales-2026-09-22.xls` |
| 文件大小 | 91,136 字节 |
| SHA-256 | `cfb597415f2c2ef23c2a00c58d2be0aa47f99ca42bd99588780f30d61ae6c28e` |
| 首次探查原件 | 同目录 `sales-2026-09-22-initial.xls`，另行保留 |
| 本地副本 | `/Users/aole/Documents/ChatGPT/vend/outputs/fanmaiji/sales-2026-09-22.xls` |
| 明细行 | 338，与本次网页查询条数一致 |
| 日期 | 338 行全部位于请求日期内 |
| 订单数 | 240 |
| 件数 | 362 |
| 明细金额合计 | 1,401.40 元 |

正式原件的本地副本与 A 机文件 SHA-256 一致。

首页对照值为 **1,388.50 元 / 239 个订单**，明细比首页多 **12.90 元 / 1 单**。原因尚未查明，原件保留，不自行删单、调整金额或据此入 ERP。日期、字段和条数校验通过，不代表首页统计口径已对平。

## 正式调度状态

- Label：`com.aole.fanmaiji-export`。
- 已安装并加载的配置：`/Users/yh-1/Library/LaunchAgents/com.aole.fanmaiji-export.plist`。
- 时间：每日 **07:30，Asia/Shanghai**；A 机 `/etc/localtime` 解析为 `Asia/Shanghai`，`RunAtLoad=false`，沿用原计划错开现有 07:00 财务任务。
- 16:29 手动触发验收后的 `launchctl`：`runs=2`，`last exit code=0`。
- 独立运行目录：`/Users/yh-1/fanmaiji-export-run`。
- 运行程序：该目录 `.venv/bin/python` 执行 `code/tools/fanmaiji-export/run_daily.py --runtime-dir /Users/yh-1/fanmaiji-export-run`。

`kickstart` 验收证明 launchd 能启动同一任务并取得成功结果，不等于已经观察到日历时间自动触发。**2026-09-24 07:30 的首次自然触发待核验**，尚无长期稳定运行结论。

运行依赖 A 机现有 Chrome 保持运行、9222 可连接且登录有效；会话失效明确失败，不自动改登录。

## 代码与离线验证

- 修正查询渲染时序：先等待初始请求完成并渲染，再等待与请求日期完全匹配的查询响应及 DOM 总条数同步，防止误取当天默认 269 条结果。
- daily 父进程按上海时区确定昨日日期，再固定传给子进程；两层互斥锁、15 分钟超时、逐次日志及状态记录保留。
- 无可复用原件时提交异步导出，处理返回的 `taskId`（含网站明确返回的同查询缓存任务），核对导出记录 ID、功能及完成状态后确认下载。
- 通过 `xlrd==2.0.2` 检查真实 XLS 必需表头、每行日期、数量及金额字段，并与本次查询条数核对，通过后才发布。
- 本次归档目录中同日期范围原件也须重新通过上述校验才可复用；校验失败保留原文件，按正常流程处理。
- 缓存复用只能保留已校验快照，**无法证明厂家在条数不变时未修订金额或状态**，不能称为每次都重新下载最新内容。
- 专用页和其派生 popup 由本次任务追踪并清理，用户既有或并行新开的其他标签页不纳入清理。
- 本地与 A 机均 **56 项测试通过**：`test_export.py` 46 项、`test_verify_sales.py` 6 项、`test_daily.py` 4 项。
- 8 个运行、配置及测试文件在本地与 A 机的 SHA-256 一致，验收对应同一份代码。
- fresh 登录、商品列表及补货流程仍为 `verified: false`，未验收、不执行。

## 尚待核验

1. 9 月 24 日 07:30 自然触发结果及对应昨日文件。
2. 明细与首页的 12.90 元 / 1 单差异。
3. ERP 上传与入账尚未执行；待数据口径和幂等性核验完成后再处理。
